# Intentionally insecure: enables directory listing on the uploads
# folder so backups / exported files placed there can be browsed.
<Directory "/var/www/html/wp-content/uploads">
    Options +Indexes
    IndexOptions FancyIndexing NameWidth=* HTMLTable
    Require all granted
</Directory>
